# Security Policy

Do not open public issues containing credentials, kubeconfig files, private images, or environment dumps. Report vulnerabilities privately through GitHub security advisories.

The committed Secret objects contain disposable local placeholders only. Replace them through a secret-management workflow before adapting this project to any shared environment. Supported code is the latest `main` branch.
